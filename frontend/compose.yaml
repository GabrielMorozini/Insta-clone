services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instagram_frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000
    restart: always